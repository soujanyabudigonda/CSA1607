# Input data
scores <- c(10, 12, 15, 18, 20, 25, 30, 35, 100)

# Boxplot
boxplot(scores,
        main = "Boxplot of Player Scores",
        ylab = "Scores",
        col = "lightblue")

# Highlight outliers
outliers <- boxplot.stats(scores)$out
cat("Outliers:", outliers, "\n")
